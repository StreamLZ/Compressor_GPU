# Vulkan Port — Risk Register (Draft)

## 1. Risk table

| ID | Severity (residual) | Risk | First detection signal | Milestone addressing | Mitigation | Owner-action-if-triggered |
|----|---------------------|------|------------------------|----------------------|------------|---------------------------|
| R1 | HIGH → Medium | `__match_any_sync` emulation regresses L1-L4 encode throughput on AMD/Intel by potentially +30% (originally HIGH) | M5 standalone microbenchmark on AMD/Intel measures site #1 + site #3 throughput vs CUDA reference with pre-committed 65% threshold | M5 (standalone retire), M9 (correctness unit tests), M18 (site #3 production), M22 (site #1 production), M28 (NV_partitioned amplifier) | NV_partitioned third SPIR-V variant on NVIDIA; 11-iter ballot loop for site #3; 32-broadcast OR-reduction for site #1; site #2 ELIMINATED (replaced by `subgroupShuffle(key4, highest_lower) == key4`, byte-identical) | If M5 measures <65% on AMD/Intel: file `streamlz-vk/issues/arch-revision-r1-amd` (or `-intel`), promote NV_partitioned-equivalent fast path from "opportunistic" to "required for Tier-1 AMD/Intel parity", PAUSE M11 (first kernel port) until arch §7 decision is revised. Acceptable fallback: lower AMD/Intel target to 50% and ship with documented perf disclosure. |
| R2 | HIGH → Medium | `subgroupSize=32` unsupportable on a meaningful mobile class (originally HIGH) | M4 vendor probe runs on lavapipe + SwiftShader + at least one real mobile borrow (Adreno or Mali via adb) at the probe-retirement milestone, not deferred to a late lane-F gate | M4 (retire via runtime probe + vendor whitelist), M29 (final validation on real Adreno + Mali) | Tier-2 catches every probe-fail device; shared-mem workgroup-wide emulation runs 32 logical lanes always; Mali (all gens) hardcoded to Tier-2 regardless of probe; pre-650 Adreno expected Tier-2 | If a Tier-1 whitelisted vendor (NVIDIA Turing+, AMD RDNA2+, Intel Arc Alchemist+) fails the M4 probe in production: file blocker, REMOVE the failing vendor from the Tier-1 whitelist in arch §3, push them to Tier-2 with documented perf penalty (25-50% CUDA), update CHANGELOG before M11 starts. If Mali Valhall shows correctness issues at M29 (not just perf): file blocker, root-cause whether emulation has a subgroup-boundary bug missed by lavapipe, fix or document mobile clamp policy. |
| R3 | BLOCKER → Low | Wire-format byte-identity regression caught late in the porting effort (originally BLOCKER) | M3 conformance harness runs 70 / 350 sanity baseline at milestone 3 of 31; every subsequent kernel milestone (M11-M27) adds stage-level byte-identity assertions; M14 (scan_parse) is the FIRST production tripwire with HARD-FAIL and per-case diff emission | M3 (harness + goldens), M14 (first production tripwire), M21 (Wave-1 gate HARD-FAIL), M27 (full conformance gate 350/350), M29-M30 (real-hardware confirmation) | Cross-backend conformance harness in PR-equivalent milestone M3; golden .slz fixtures committed; lavapipe + SwiftShader value-path CI; real-mobile lane F + real-CDNA lane G for subgroup-boundary semantics; M0 CUDA-side highest-lane-winner rewrite makes byte-identity portable | If ANY kernel milestone (M11-M27) finds a byte-identity divergence: per-case diff committed to `tests/diffs/{case}_L{n}_{kernel}.diff`, milestone FAILS, no advancement until root-caused and fixed. If divergence appears at lane F or G post-M27: BLOCKER for v1.0-alpha; either fix or document Tier-2 mobile/CDNA clamp policy. |
| R4 | HIGH → Low | BDA absent breaks `void*` device-pointer ABI on mobile (originally HIGH) | M2 ships `slzRegisterBuffer_vk` as no-op stub returning SLZ_SUCCESS at milestone 2 — ABI is parity from day 1; M6 validates BDA round-trip on every Tier-1 vendor | M2 (ABI parity), M6 (BDA round-trip), M11 (first production kernel uses BDA on Tier-1, descriptor binding on Tier-2 via registerBuffer) | `slzRegisterBuffer_vk` 15th symbol exported unconditionally on both tiers (no-op on Tier-1 where BDA is present, mandatory on Tier-2); raw `void*` C ABI unchanged from CUDA; portable callers always call register | If M6 finds BDA broken on a Tier-1 whitelisted vendor (e.g., a specific driver version returns garbage from `buffer_reference` deref): pin minimum driver version in `docs/vulkan_port_architecture.md` §4, document in CHANGELOG, and if necessary push the broken driver to Tier-2 via a runtime driver-version check at M4 probe. |
| R5 | HIGH → Low | Encode hash table > 2 GiB on single VkBuffer fails (originally HIGH) | M7 stress test allocates 3 × 2 GiB hash buffers on a 24 GB-VRAM device with a synthetic kernel that BDA-addresses across buffer boundaries; gated to "skipped with INFO log" on <8 GB devices but always runs on CI lane with sufficient VRAM | M7 (bin-pack design + stress test), M22 (production validation in lz_encode at L5 on synthetic 64 MiB input) | Bin-pack sub-chunks across N ≤2 GiB VkBuffers; no single sub-chunk's hash crosses a buffer boundary; Tier-2 worst-case descriptor sizing at handle create (N=8 mobile clamp ceiling); slab allocator never exceeds 2 GiB per VkBuffer | If M7 stress test fails on a 24 GB device: revisit split-math, possibly reduce sub-chunk size, or document a maximum input size for v1.0-alpha. If a real workload at M22 exceeds the bin-pack ceiling: clamp the encode level on Tier-2 (effective_level_out reported via reserved[0]), surface via stderr per arch §6. |
| R6 | LOW | HOST_CACHED\|HOST_VISIBLE memory type missing on some Vulkan devices halves D2H bandwidth | M7 memory allocator probes for HOST_CACHED and reports `cached_readback=false` when absent | M7 (fallback in allocator) | HOST_VISIBLE\|HOST_COHERENT baseline + staging-transfer fallback | Document the device + perf delta in `docs/vulkan_port_architecture.md` §22; no code change needed. |
| R7 | LOW | `VkPipelineCache` cold-start adds 100-500 ms warm-start latency | M8 pipeline chassis measures cold vs warm start; M28 shape-keyed cmd buffer cache further reduces small-input dispatch latency | M8 (process-wide cache per `(deviceUUID, tier)` on disk), M28 (shape-keyed cmd cache amplifier) | Cache persists to `%LOCALAPPDATA%/Cache/streamlz_vk/pipelines_{UUID}_t{tier}.bin`; warm-start target 3-5 ms | If cold-start exceeds 1 second on any platform: investigate per-pipeline compile times, possibly emit precompiled cache fixtures for first-run experience. |
| R8 | LOW | Cross-stream barrier elision regression introduces races | M8 sync chassis uses single VkQueue (collapses cross-stream to intra-cmd-buffer barriers) + explicit per-buffer `VkBufferMemoryBarrier2` between dependent dispatches; validated by sync2 layer | M8 (sync design), M12+ (each kernel milestone validates barriers under `SLZ_VK_VALIDATION=1`) | Single VkQueue per `slzHandle_vk_t` (locked decision #5); sync2 → sync1 fallback when synchronization2 not promoted | If validation layer reports a race at any kernel milestone: trace to missing/elided barrier; add explicit barrier; no design change needed. |
| R9 | Medium → Low | 2-warp LZ-decode coupling under workgroup-wide barrier could deadlock on Tier-2 | M12 (lz_decode_raw) and M20 (lz_decode) validate the Tier-2 2× workgroup collapse policy on lavapipe + SwiftShader; M29 confirms on real Adreno/Mali | M12 (Tier-2 collapse policy validated on raw path), M20 (validated on main LZ decode), M29 (real-hardware confirmation) | Tier-1 keeps `local_size_x=64` with per-subgroup `subgroupBarrier()`; Tier-2 collapses to `local_size_x=32` with 2× workgroup dispatch using workgroup-wide `barrier()` | If Tier-2 deadlocks on real Mali at M29: confirm SwiftShader missed the bug (subgroup-spanning semantics differ); fix to never cross a workgroup boundary in the inner loop. |
| R10 | LOW | float→uint determinism for non-{0.25, 0.5} sc_group_size | M4 host-side pre-quantization to integer products 65536 / 131072; M14 (scan_parse) validates no f32 in SPIR-V via `spirv-dis | Select-String "OpTypeFloat"` | M4 (host-side quantization), M14 (SPIR-V audit), M25 (assemble_measure consumes integer) | Pre-quantize host-side; no f32 ever crosses the SPIR-V boundary; SpecConstant integer values in wire_format.glsl | If any future SC group size value needs to be supported: extend the integer-quantization table host-side; never use f32 inside shaders. |
| R11 | LOW | Mobile shared-mem `atomicAdd` serialization differences on tile-based GPUs | M23 (huff_build_tables) validates commutative-safe histograms on lavapipe (simulating tile-based GPU); M29 confirms on real Mali (tile-based architecture) | M23 (production validation), M29 (real-hardware confirmation) | Only `atomicAdd` on `__shared__`; histograms commutative-safe (deep-dive resolved); hash-store collisions resolved via explicit highest-lane-winner from M0, NOT implicit atomic order | If a real Mali run at M29 produces non-deterministic histograms: investigate whether `atomicAdd` on shared-mem has different semantics; replace with a serial reduction if needed. |
| R12 | Medium → Low | Regrow OOM on mobile during memory clamp | M0 introduces `effective_level_out` ABI slot; Tier-2 writes post-clamp level; both CLIs surface clamps via stderr; M31 documents clamp behavior in README | M0 (ABI), M4 (clamp policy in slzCreate), M31 (docs) | Graceful clamp to lower level (L5→L3); `effective_level_out` in `reserved[0]` reports actual level; caller knows | If a real mobile run at M29 OOMs without clamping: tighten the clamp threshold; possibly add additional clamp paths (L3→L2, L2→L1); document supported mobile VRAM minimums. |

## 2. R1-R5 detailed playbooks

### R1: `__match_any_sync` emulation perf on AMD/Intel

**Why it matters.** The Vulkan port must approximate CUDA encode throughput on AMD and Intel Arc to be a credible alternative to the CUDA backend. CUDA's `__match_any_sync` is one PTX instruction; the Vulkan emulation costs 11+ ballot iterations (site #3) or a 32-broadcast OR-reduction (site #1). If the emulation regresses encode throughput by >35% on AMD/Intel, the Vulkan port is shippable but uncompetitive for production use on those vendors, which threatens the "full vendor coverage" locked constraint.

**Detection.** M5 stands up a STANDALONE microbenchmark — port JUST the two match_any emulations (site #1 and site #3) as `.comp` shaders with no other kernel logic. Run on every CI lane. Measure ns/dispatch on synthetic workloads sized to match production huff_build_lut (site #3) and lz_encode (site #1) shapes. Compare to CUDA reference baseline running the same workload sizes. **Pre-committed numeric threshold: AMD throughput ≥ 65% of CUDA on BOTH sites; same for Intel.**

**Playbook if triggered.**
1. Confirm the measurement: re-run on three different AMD/Intel devices in the CI matrix to rule out a one-off driver issue.
2. Profile the SPIR-V via `spirv-cross` and AMD/Intel native profilers (RGP / VTune Graphics) to identify the specific stall.
3. If the stall is in the 11-iter ballot loop (site #3): investigate `subgroupBallotBitCount` lowering on the failing vendor; possibly replace with a manual `bitfieldExtract` loop.
4. If the stall is in the 32-broadcast OR-reduction (site #1): promote `subgroupPartitionedXorNV`-equivalent fast path from "opportunistic" (NVIDIA only) to "required for Tier-1 AMD/Intel parity" — investigate whether `VK_AMD_shader_ballot` or `VK_INTEL_*_subgroup_*` extensions offer equivalent primitives.
5. If no fast path exists for AMD/Intel: file `streamlz-vk/issues/arch-revision-r1-{vendor}`, revise arch §7 to lower the AMD/Intel perf target from 65% to 50%, document the deviation in CHANGELOG, ship v1.0-alpha with a clear perf disclosure in README.
6. PAUSE M11 (first kernel port) until decision is documented in `docs/vulkan_port_architecture.md` §7.

**Why we measure now and not later.** If the standalone measurement at M5 is fine but a later production-kernel measurement (M18/M22) shows worse perf, the problem is integration overhead (other code in the kernel), not emulation cost — and is fixable inside the consuming kernel without re-architecting. If the standalone measurement at M5 is below threshold, the problem is fundamental and warrants pausing.

### R2: `subgroupSize=32` unsupportable on mobile

**Why it matters.** Tier-1 requires `subgroupSize=32` pinned via `VK_EXT_subgroup_size_control`. If a Tier-1 whitelisted vendor (e.g., a future AMD driver, or an Intel Arc revision) silently fails to honor the pin at runtime, Tier-1 kernels execute with wrong lane semantics and produce incorrect bytes. Mali is hardcoded to Tier-2 (no Mali gen advertises 32 in min/maxSubgroupSize), but Adreno is more uncertain.

**Detection.** M4 vendor probe runs at every `slzCreate_vk` call. The probe dispatches a one-warp kernel that reads `gl_SubgroupSize` via `subgroupBroadcastFirst` into a host-visible buffer. If the value is not 32 OR shuffle behavior is wrong on a Tier-1 whitelisted device, the probe falls through to Tier-2. **Crucially, M4 also runs the probe on at least one real mobile borrow** (Pixel 8 Adreno or Pixel 6 Mali via adb-attached CI worker, when available) to validate the whitelist BEFORE Wave 1 starts.

**Playbook if triggered.**
1. Reproduce on the failing device — capture `vkGetPhysicalDeviceProperties2` output for the device.
2. Check whether the device supports `VK_EXT_subgroup_size_control` with `subgroupSizeControl=VK_TRUE` AND the range includes 32. If the range does not include 32, the device cannot ever be Tier-1; remove from whitelist.
3. If the device claims subgroupSize=32 but the probe kernel reads a different value: file a driver bug AND temporarily push the device to Tier-2 via a runtime driver-version check at M4 probe.
4. If a real Mali run at M29 shows correctness divergence from lavapipe Tier-2: lavapipe missed a subgroup-boundary semantic; fix the Tier-2 emulation to never assume specific subgroup boundaries (use workgroup-wide `barrier()` exclusively, never `subgroupBarrier()`).
5. Update `docs/vulkan_port_architecture.md` §3 device matrix with the new Tier-1/Tier-2 line; update CHANGELOG.

### R3: Wire-format byte-identity regression caught late

**Why it matters.** The whole project promise is "byte-identical .slz at L1..L5 between CUDA and Vulkan". A regression caught at M27 (full conformance gate) is enormously expensive to root-cause because dozens of kernels are now in the pipeline. A regression caught at M14 (scan_parse, the first byte-identity tripwire) is cheap.

**Detection.** M3 conformance harness lands at milestone 3 of 31 with 70 / 350 sanity baseline. Every kernel milestone (M11-M27) adds a stage-level byte-identity assertion that compares the kernel's intermediate output to CUDA byte-for-byte. M14 (scan_parse) is the FIRST production tripwire with HARD-FAIL and per-case diff emission to `tests/diffs/{case}_L{n}_scan_parse.diff`.

**Playbook if triggered.**
1. The kernel that introduced the divergence is the kernel currently being ported — diff against the previous milestone's output to confirm.
2. Categorize the divergence: (a) endianness (3-byte big-endian header read with wrong byte order), (b) alignment (off16/off32 boundary), (c) wire-format constant mismatch (HUFF_NUM_STREAMS, magic, version, codec), (d) sc_group_size float-vs-integer (R10 leak), (e) match_any emulation produces different lane election (R1 correctness bug, not perf), (f) shared-mem atomicAdd order divergence (R11 — but should be commutative-safe, so this would be a true bug).
3. Most divergences are (a) or (b) — fix in the GLSL source, regenerate SPV, re-run.
4. If divergence is (e), validate against M5's standalone benchmark output — does it agree? If not, the emulation regressed; if yes, the integration in the kernel introduced a bug.
5. Do not advance to the next kernel milestone until the diff is clean.
6. If divergence is found at lane F (real mobile) or lane G (real CDNA) at M29/M30: BLOCKER for v1.0-alpha. Either fix or document an explicit Tier-2 mobile/CDNA clamp policy in CHANGELOG.

### R4: BDA absent breaks `void*` device-pointer ABI

**Why it matters.** The CUDA C ABI passes raw `void*` device pointers. Tier-1 Vulkan can use `VK_KHR_buffer_device_address` to read those pointers directly. Tier-2 Vulkan must use descriptor-set bindings, which requires the caller to register the buffer before use — a 15th C ABI symbol.

**Detection.** M2 ships `slzRegisterBuffer_vk` as a no-op stub returning SLZ_SUCCESS from day 1, so the ABI is parity from milestone 2. M6 validates BDA round-trip on every Tier-1 vendor (NVIDIA, AMD, Intel) via a small `buffer_reference` deref test.

**Playbook if triggered.**
1. If M6 finds BDA broken on a Tier-1 whitelisted vendor (e.g., a specific driver version returns garbage from `buffer_reference` deref): file a driver bug.
2. Pin minimum driver version in `docs/vulkan_port_architecture.md` §4 (extensions section).
3. Document in CHANGELOG which driver versions are supported.
4. If necessary, push the broken driver to Tier-2 via a runtime driver-version check at M4 probe — Tier-2 doesn't use BDA, so the broken driver path is avoided.
5. If the broken driver is widely deployed (e.g., a recent NVIDIA driver): consider shipping with a `SLZ_VK_DISABLE_BDA=1` env var that forces Tier-2 path on Tier-1-capable hardware.

### R5: Encode hash table > 2 GiB on single VkBuffer

**Why it matters.** Vulkan caps `VkBufferCreateInfo.size` at `maxStorageBufferRange`, which is 2 GiB on many GPUs. L5 encode on a 24 GB input produces a ~3.5 GiB hash table — won't fit in one buffer.

**Detection.** M7 stress test allocates 3 × 2 GiB hash buffers on a 24 GB-VRAM device with a synthetic kernel that BDA-addresses across buffer boundaries via a push-constant pointer table. Gated to "skipped with INFO log" on <8 GB devices (developer laptops) but always runs on a CI lane with sufficient VRAM.

**Playbook if triggered.**
1. If M7 stress test fails on a 24 GB device: investigate whether the bin-pack math correctly avoids cross-buffer sub-chunks; verify the descriptor-set layout supports the worst-case N=8 buffer count on Tier-2.
2. If the failure is in the BDA push-constant table (Tier-1): push-constant size limit is 128 B per arch §4; verify the table fits in 128 B (8 × 16 B = 128 B for 8 buffers; tight).
3. If a real workload at M22 (lz_encode) exceeds the bin-pack ceiling at L5: clamp the encode level on Tier-2 (effective_level_out reported via `reserved[0]`); surface via stderr per arch §6.
4. Update `docs/vulkan_port_architecture.md` §6 split math with the failure case + workaround.

## 3. Newly-identified risks from milestone planning

| ID | Severity | Risk | Mitigation in milestone plan |
|----|----------|------|------------------------------|
| N1 | Medium | M0 byte-equivalence assumption may fail on future NVIDIA drivers | M0 acceptance criteria requires post-rewrite goldens SHA-256-match pre-rewrite goldens. If they don't match on a future driver, M0 fails and triggers stakeholder review (re-baseline goldens with documented rationale, or revise the rewrite). Better to discover at M0 than at M22. |
| N2 | Low | M3 conformance harness "expected-XFAIL count matches actual" discipline could mask a regression if the expected-XFAIL count is updated incorrectly | The expected-XFAIL count is computed automatically from the milestone-to-cells lookup table committed in M3; it's not a manually-maintained number. Each kernel milestone updates the lookup table; CI rejects PRs that change the count without updating the table. |
| N3 | Low | Real-mobile lane F may be unavailable for extended periods (phone broken, adb dropped, battery dead) | Lane F runs nightly, not per-PR. PR gating only requires lanes A, B, C, E, B' green. Lane F failure files a P1 issue but doesn't block PRs. Lane F is gated as a v1.0-alpha RELEASE blocker (M31), not as a per-PR gate. |
| N4 | Low | Real AMD CDNA lane G is cloud-rented; cost may bound how often it runs | Lane G runs weekly. Same release-blocker (not per-PR) policy as lane F. If cost is prohibitive, run on every Wave-2+ milestone (M22-M27) at minimum. |
| N5 | Medium | sc_group_size pre-quantization (R10) is centralized in M4 but every kernel that reads sc_group_size needs to know the integer encoding | M9 `wire_format.glsl` exports the integer constants as SpecConstants; every kernel that reads sc_group_size includes wire_format.glsl. M14 (scan_parse) audit verifies no `OpTypeFloat` appears in any committed SPV via `spirv-dis | Select-String "OpTypeFloat"` returning zero matches across all 51 production SPVs. |
| N6 | Low | M22 lz_encode is 1500 LOC — largest milestone in the plan; if it doesn't land cleanly in one PR, the binary's Wave-1 gate (M21) functionality is preserved but Wave-2 is blocked | Allow M22 to land as a sequence of internal commits within the milestone; the milestone-completion gate is when all 70 fixtures × L1..L5 pass on Tier-1 + Tier-2. Use feature flags inside lz_encode.comp to incrementally enable L1, L2, L3, L4, L5 paths. |
| N7 | Low | The 51 committed .spv blobs (~1-2.5 MiB total) bloat repo size; future kernel changes require regenerating all 51 | `tools/build_glsl.bat` is idempotent (mtime-based); CI verifies committed SPVs match what `build_glsl.bat` produces from current GLSL sources. If they diverge, CI fails the PR. Optionally compress .spv blobs in git (zstd) — defer to v1.1. |
| N8 | Medium | Site #2 elimination (replaced by `subgroupShuffle(key4, highest_lower) == key4` per arch §10) is byte-identical in principle but not unit-tested independently | Add a dedicated test in M9 `match_any_unit_test.comp` that exercises the site #2 replacement pattern on a synthetic input and asserts byte equality against a CPU reference. If divergence found: arch §10 needs revision before M22 uses the pattern. |
| N9 | Low | "Always-shippable" property between M22 (encode starts working partially) and M27 (encode fully working) means `streamlz_vk.exe compress` may produce CORRECT bytes for L1 but WRONG bytes for L4/L5 if M22 ships incrementally | Until M22 lands all 5 levels green, `slzCompressAsync_vk` returns `SLZ_ERROR_UNSUPPORTED_LEVEL` for any level not yet known-good. CLI surfaces this with a clear error. The set of "known-good levels" is read from a compile-time table that updates as M22's internal commits land. |
| N10 | Medium | M28 NV_partitioned validation depends on real NVIDIA Turing+ hardware in CI; if unavailable, the tier1_nv/ SPV blobs ship without runtime validation | Gate NV_partitioned variant selection on a probe-time check (`VK_NV_shader_subgroup_partitioned` exposed + a small byte-identity sanity dispatch at `slzCreate_vk`); if the sanity dispatch fails, fall back to the base tier1/ variant. This makes NV_partitioned a runtime opt-in even if M28 can't fully validate it. |
| N11 | Low | SwiftShader Tier-1 emulation (lane B' as a Tier-1 lane, mentioned in some plans) likely cannot pin subgroupSize=32 reliably; relying on it as a Tier-1 CI lane could mask shipability failures with infrastructure issues | Plan uses SwiftShader ONLY as a Tier-2 wave64 lane (forced via `VK_SUBGROUP_SIZE=64`); never as a Tier-1 lane. Real Tier-1 validation comes from lane A (NVIDIA, when available) and lane B (AMD, when available); if neither is available, Tier-1 acceptance criteria are downgraded to "post-merge validation on borrowed hardware" with a P1 issue tracking the missing CI lane. |
